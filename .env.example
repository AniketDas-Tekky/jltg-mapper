# Root dev-environment example.
#
# The backend reads its own config from backend/.env (prefix JLTG_). Copy that file:
#   cp backend/.env.example backend/.env
#
# The values below mirror the local Docker Postgres started by `make db-up`
# (see docker-compose.yml). Keep them in sync with backend/.env.example.

# Local Postgres (matches docker-compose.yml: user/pass/db = jltg/jltg/jltg)
POSTGRES_USER=jltg
POSTGRES_PASSWORD=jltg
POSTGRES_DB=jltg
POSTGRES_PORT=5432

# Backend connection string (SQLAlchemy + psycopg). Mirrors backend/.env.example.
JLTG_DATABASE_URL=postgresql+psycopg://jltg:jltg@localhost:5432/jltg
